import {AppError,type Principal} from '@runlumi/core/contracts.ts';
type Jwk=JsonWebKey&{kid?:string};
const cache=new Map<string,{expires:number;keys:Jwk[]}>();
function decode(part:string):Uint8Array{if(!/^[A-Za-z0-9_-]+$/.test(part))throw new AppError(401,'INVALID_TOKEN');try{return Uint8Array.from(atob(part.replace(/-/g,'+').replace(/_/g,'/')),c=>c.charCodeAt(0))}catch{throw new AppError(401,'INVALID_TOKEN')}}
function object(part:string):Record<string,unknown>{const value=JSON.parse(new TextDecoder('utf-8',{fatal:true}).decode(decode(part)));if(!value||typeof value!=='object'||Array.isArray(value))throw new AppError(401,'INVALID_TOKEN');return value as Record<string,unknown>}
export async function verifyAccessToken(token:string,team:string,audience:string,fetcher:typeof fetch=fetch,nowSeconds=Math.floor(Date.now()/1000)):Promise<Principal>{
 if(!/^[a-z0-9][a-z0-9-]{0,62}$/.test(team)||!/^[A-Za-z0-9_-]{20,128}$/.test(audience)||/REPLACE|example/i.test(team+audience))throw new AppError(503,'AUTH_NOT_CONFIGURED');try{if(token.length>16384)throw new Error('size');const parts=token.split('.');if(parts.length!==3)throw new Error('parts');const header=object(parts[0]!),claims=object(parts[1]!);if(header.alg!=='RS256'||typeof header.kid!=='string'||header.crit!==undefined||header.jku!==undefined||header.jwk!==undefined)throw new Error('header');const issuer=`https://${team}.cloudflareaccess.com`,audiences=Array.isArray(claims.aud)?claims.aud:[claims.aud];if(claims.iss!==issuer||!audiences.every(value=>typeof value==='string')||!audiences.includes(audience)||typeof claims.sub!=='string'||!claims.sub||claims.sub.length>256)throw new Error('claims');if(!Number.isSafeInteger(claims.exp)||(claims.exp as number)<=nowSeconds)throw new Error('expired');if(claims.nbf!==undefined&&(!Number.isSafeInteger(claims.nbf)||(claims.nbf as number)>nowSeconds+30))throw new Error('nbf');if(claims.iat!==undefined&&(!Number.isSafeInteger(claims.iat)||(claims.iat as number)>nowSeconds+30))throw new Error('iat');let entry=cache.get(issuer);if(!entry||entry.expires<=nowSeconds){const response=await fetcher(`${issuer}/cdn-cgi/access/certs`,{redirect:'error',signal:AbortSignal.timeout(5000)});if(!response.ok)throw new Error('jwks');const payload=JSON.parse(await response.text()) as {keys?:Jwk[]};if(!Array.isArray(payload.keys)||payload.keys.length>32)throw new Error('jwks');entry={expires:nowSeconds+300,keys:payload.keys};cache.set(issuer,entry)}const keys=entry.keys.filter(key=>key.kid===header.kid&&key.kty==='RSA'&&(!key.alg||key.alg==='RS256')&&(!key.use||key.use==='sig'));if(keys.length!==1)throw new Error('key');const key=await crypto.subtle.importKey('jwk',keys[0]!,{name:'RSASSA-PKCS1-v1_5',hash:'SHA-256'},false,['verify']);if(!await crypto.subtle.verify('RSASSA-PKCS1-v1_5',key,decode(parts[2]!),new TextEncoder().encode(`${parts[0]}.${parts[1]}`)))throw new Error('signature');return {issuer,subject:claims.sub as string}}catch{throw new AppError(401,'UNAUTHENTICATED')}
}
