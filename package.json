{
  "name": "@runlumi/bi",
  "version": "0.1.8",
  "private": true,
  "license": "Elastic-2.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22.16.0"
  },
  "scripts": {
    "dev": "node --experimental-strip-types scripts/dev.mjs",
    "test": "node --experimental-strip-types --test tests/*.test.mjs",
    "typecheck": "tsc --noEmit",
    "build:packages": "npm run build --workspace @runlumi/core && npm run build --workspace @runlumi/cloudflare && npm run build --workspace @runlumi/ui",
    "check": "npm run build:packages && npm run typecheck && npm test && node --experimental-strip-types scripts/check-repo.mjs && npm run check:boundaries && npm run check:licensing",
    "init:github": "node --experimental-strip-types scripts/init-github.mjs",
    "dev:web": "npm --prefix apps/web run dev",
    "build:web": "npm --prefix apps/web run build && node scripts/web-notices.mjs",
    "check:core": "npm run build:packages && tsc --noEmit && npm test",
    "setup": "npm ci --ignore-scripts && npm --prefix apps/web ci --ignore-scripts",
    "predev": "npm run build:web",
    "check:web-deps": "node scripts/check-web-deps.mjs",
    "check:licensing": "node scripts/check-licensing.mjs",
    "check:boundaries": "node scripts/check-boundaries.mjs",
    "core:pack": "node scripts/core-pack.mjs",
    "refresh:customer-lock": "node scripts/refresh-customer-lock.mjs",
    "customer:new": "node scripts/customer-new.mjs"
  },
  "description": "Standalone installation BI for trusted operational decisions.",
  "devDependencies": {
    "@types/node": "22.18.6",
    "wrangler": "4.123.0",
    "typescript": "5.8.3"
  }
}
