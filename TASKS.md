# Current work

- Direct password sign-in, one-time protected setup and last-owner protection are
  shipped and tested (auth suite + strict-mode browser journey).
- Two-repository package-consumer acceptance (`scripts/acceptance-two-customers.mjs`)
  and the workerd runtime check (`scripts/workerd-check.mjs`) run in CI.
- Backup/restore and rollback are documented operator procedures (user-guide 12).
